import org.junit.jupiter.api.Test;
import signature.SignatureOptionsBuilder;

import static org.hamcrest.MatcherAssert.assertThat;

class SignatureSearcherTest {
    /*@Test
    public void test(){
        final var searcher = new SignatureOptionsBuilder();
        final int maximumNumberOfOptions = 1;
        final int targetNumberOfPages = 25;
        final int minimumSheetsPerSignature = 3;
        final int maximumSheetsPerSignature = 5;
        final var options = searcher.build(targetNumberOfPages,
                maximumNumberOfOptions,
                minimumSheetsPerSignature,
                maximumSheetsPerSignature);
        assertThat(options.numberOfOptions(),equals(1));
    }*/
}